appId: com.example.bbplayer
productName: BBPlayer-desk
directories:
  output: dist
files:
  - out/**
  - package.json
asar: true
npmRebuild: true
win:
  target:
    - target: portable
      arch: [x64]
portable:
  artifactName: ${productName}-${version}-portable.${ext}
