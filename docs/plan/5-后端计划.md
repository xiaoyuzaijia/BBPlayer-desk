---
title: BBPlayer-desk 后端逻辑层计划(Electron 主进程版)
description: 基于 Electron 架构,主进程承载所有第三方数据获取与本地持久化,1:1 复刻 BBPlayer 后端逻辑
date: 2026-07-20
---

## 0. 计划目标与定位

### 0.1 关键决策

**直接采用 Electron 架构开发**,所有"后端逻辑"(第三方 API 调用、本地持久化、业务编排)放在**主进程**(Node.js 环境),渲染进程(Vue 应用)只负责 UI 和 UI 状态。

**不做的事**:
- 不做自有云后端(无 Hono / Workers / Supabase / JWT / 账号系统)
- 不做共享歌单协作(无 outbox / LWW / 邀请码)
- 不做应用更新检查(electron-updater 留作可选)
- 不做 web 部署(纯 Electron 桌面应用)

**要做的事**:
- 在主进程 1:1 复刻 BBPlayer 的 B 站 API client(WBI 签名 + cookie 注入 + 30+ 接口)
- 在主进程 1:1 复刻 BBPlayer 的本地持久化(`better-sqlite3` + Drizzle ORM + SQLite)
- 在主进程 1:1 复刻 BBPlayer 的 service / facade 编排模式
- 在主进程管理 B 站 cookie(`tough-cookie` 库)
- 在主进程实现多源歌词获取(网易云 / QQ 音乐 / 酷狗)
- 通过 IPC + preload contextBridge 暴露给渲染进程

### 0.2 为何直接 Electron(而非 web → WASM → Electron 三步走)

| 维度 | 直接 Electron | 先 web 再迁移 |
|---|---|---|
| BBPlayer 代码复用度 | **几乎 1:1 复刻**(SQLite + Drizzle 都能用) | IndexedDB 与 SQLite 查询能力差一个数量级,service 层要重写两次 |
| CORS 问题 | **无**(主进程发请求不受同源策略) | 需 Vite proxy + CF Workers 透传 |
| User-Agent 限制 | **无**(`session.webRequest` 可任意改) | 浏览器禁止修改 |
| Set-Cookie 限制 | **无**(Node `https` 响应头完全可读) | 浏览器禁止读取 |
| 数据库 | **真 SQLite**(`better-sqlite3`,同步 API,性能极强) | IndexedDB 或 SQLite WASM(兼容性/性能/体积都有代价) |
| ORM | **Drizzle ORM**(与 BBPlayer 完全一致,关系查询全可用) | Drizzle 在 IndexedDB 上不可用 |
| 部署复杂度 | 安装包 70-100MB | web 部署简单,但能力打折 |
| 学习曲线 | Electron 主进程 + IPC + 打包 | 仅 web,但后期还要再学一遍 Electron |

**结论**:直接上 Electron,**总工作量比"先 web 再迁移"少一半**,且代码质量更高(1:1 复刻 vs 两次重写)。

### 0.3 调研基础

详见 [4-BBPlayer前后端分层调研.md](./4-BBPlayer前后端分层调研.md)。

---

## 1. Electron 架构总览

### 1.1 三进程模型

```
┌──────────────────────────────────────────────────────────────┐
│  Electron 应用                                               │
│                                                              │
│  ┌────────────────────┐  IPC  ┌─────────────────────────┐    │
│  │   主进程 (Node.js) │←────→│  渲染进程 (Chromium)    │    │
│  │                    │       │                         │    │
│  │  - B 站 API client │       │  - Vue 3 应用           │    │
│  │  - SQLite + Drizzle│       │  - Pinia(UI 状态)      │    │
│  │  - services        │       │  - TanStack Query       │    │
│  │  - facades         │       │  - <audio> 播放         │    │
│  │  - cookie jar      │       │  - UI 组件              │    │
│  │  - 本地音频代理    │       │                         │    │
│  │  - 模块级状态      │       │                         │    │
│  └────────────────────┘       └─────────────────────────┘    │
│              ↑                                                │
│              │                                                │
│       ┌──────┴───────┐                                        │
│       │  preload     │  contextBridge.exposeInMainWorld       │
│       │  (隔离环境)  │  → window.api.xxx()                    │
│       └──────────────┘                                        │
└──────────────────────────────────────────────────────────────┘
```

### 1.2 各进程职责

| 进程 | 环境 | 职责 |
|---|---|---|
| **主进程** | Node.js | 所有"后端逻辑":API 调用、数据库、业务编排、cookie 管理、音频流代理 |
| **渲染进程** | Chromium | Vue 应用、UI 组件、Pinia 状态、TanStack Query、`<audio>` 播放 |
| **preload** | 隔离 JS 环境 | 通过 `contextBridge` 安全暴露主进程 API 给渲染进程 |

### 1.3 数据流向

```
用户操作 → Vue 组件 → composable → window.api.xxx() → preload contextBridge
                                                              ↓
                                                          ipcRenderer.invoke
                                                              ↓
                                                          ipcMain.handle
                                                              ↓
                                                          facade
                                                              ↓
                                              ┌───────────────┴───────────────┐
                                              ↓                               ↓
                                          service                          API client
                                              ↓                               ↓
                                          SQLite (better-sqlite3)        B 站 API (https)
```

返回值原路返回。TanStack Query 在渲染进程侧负责缓存,主进程不知道缓存存在。

---

## 2. 目录结构

### 2.1 完整目录树

采用 **electron-vite 默认布局**:一个 `src/` 顶层,按进程切分为 `main` / `preload` / `renderer` / `shared` 四个平级目录,每个目录对应一个独立 tsconfig。这是 Electron 生态主流结构,IDE 友好,`shared` 与三进程平级避免渲染进程"穿透" main 目录。

```
BBPlayer-desk/
├── package.json
├── electron.vite.config.ts          ← electron-vite 配置(主/渲染/preload 三端构建)
├── electron-builder.yml             ← 打包配置
├── tsconfig.json                    ← 根 tsconfig(references 引用下面两个)
├── tsconfig.node.json               ← 主进程 + preload(Node 环境)
├── tsconfig.web.json                ← 渲染进程(DOM 环境)
│
├── src/
│   ├── main/                        ← 主进程(Node 环境)
│   │   ├── index.ts                 ← app 启动入口
│   │   ├── window.ts                ← BrowserWindow 创建与管理
│   │   ├── ipc/                     ← IPC handler 注册
│   │   │   ├── index.ts             ← 统一注册入口
│   │   │   ├── auth.ts              ← 登录/登出/用户信息
│   │   │   ├── playlist.ts          ← 歌单 CRUD
│   │   │   ├── track.ts             ← 曲目 CRUD
│   │   │   ├── bilibili.ts          ← B 站 API 调用代理
│   │   │   ├── lyric.ts             ← 歌词获取
│   │   │   ├── playback.ts          ← 播放编排(拉音频流 URL)
│   │   │   └── history.ts           ← 播放历史
│   │   ├── lib/                     ← 后端逻辑层(1:1 复刻 BBPlayer apps/mobile/src/lib/)
│   │   │   ├── api/clients/
│   │   │   │   ├── bilibili/
│   │   │   │   │   ├── client.ts    ← ApiClient(fetch + cookie 注入)
│   │   │   │   │   ├── api.ts       ← 30+ B 站接口封装
│   │   │   │   │   ├── wbi.ts       ← WBI 签名算法
│   │   │   │   │   ├── utils.ts     ← bv2av / av2bv / CSRF 提取
│   │   │   │   │   └── index.ts
│   │   │   │   ├── netease/         ← 网易云 API client
│   │   │   │   ├── qqmusic/         ← QQ 音乐 API client
│   │   │   │   ├── kugou/           ← 酷狗 API client
│   │   │   │   └── index.ts
│   │   │   ├── services/            ← 单表 CRUD(Drizzle + better-sqlite3)
│   │   │   │   ├── playlistService.ts
│   │   │   │   ├── trackService.ts
│   │   │   │   ├── artistService.ts
│   │   │   │   ├── bilibiliMetadataService.ts
│   │   │   │   ├── lyricService.ts
│   │   │   │   ├── historyService.ts
│   │   │   │   ├── queueService.ts
│   │   │   │   └── preferenceService.ts
│   │   │   ├── facades/             ← 跨资源业务编排
│   │   │   │   ├── playback.ts      ← 播放编排(拉音频流 + 写状态 + 通知渲染)
│   │   │   │   ├── bilibiliPlaylist.ts   ← 收藏夹/合集/UP 主投稿同步
│   │   │   │   ├── externalPlaylist.ts   ← 外部歌单匹配 B 站
│   │   │   │   └── lyricSync.ts     ← 歌词同步(用户拖动防抖)
│   │   │   ├── db/                  ← 数据库层
│   │   │   │   ├── schema.ts        ← Drizzle schema(1:1 复刻 BBPlayer)
│   │   │   │   ├── index.ts         ← createDb 工厂
│   │   │   │   └── migrations/      ← SQL 迁移文件
│   │   │   ├── auth/
│   │   │   │   ├── cookieJar.ts     ← tough-cookie 管理 B 站 cookie
│   │   │   │   └── qrLogin.ts       ← 扫码登录轮询逻辑
│   │   │   ├── config/
│   │   │   │   ├── store.ts         ← 模块级单例状态(替代 Zustand)
│   │   │   │   └── queryClient.ts   ← (仅类型参考,实际 queryClient 在渲染进程)
│   │   │   ├── audio/
│   │   │   │   └── streamProxy.ts   ← 本地 HTTP server,代理 B 站音频流
│   │   │   └── utils/
│   │   │       ├── crypto.ts        ← md5 等(WBI 签名用)
│   │   │       └── serialize.ts     ← cookie 序列化
│   │   └── types.ts                 ← 主进程内部类型
│   │
│   ├── preload/
│   │   └── index.ts                 ← contextBridge 暴露 API
│   │
│   ├── renderer/                    ← 渲染进程(Vue 应用,现有 src/ 内容整体搬这里)
│   │   ├── components/
│   │   │   ├── auth/                ← QrLoginModal / CookieLoginModal
│   │   │   ├── layout/              ← Sidebar / NowPlayingBar
│   │   │   ├── player/              ← PlayerProgressBar / QueueDrawer
│   │   │   ├── playlist/            ← PlaylistHeader / TrackList / TrackListItem
│   │   │   ├── lyric/               ← LyricView / LyricLineItem
│   │   │   └── common/              ← IconButton / CoverPlaceholder / MD3Slider / MD3Switch
│   │   ├── views/
│   │   │   ├── HomeView.vue
│   │   │   ├── LibraryView.vue
│   │   │   ├── PlaylistView.vue
│   │   │   ├── PlayerView.vue
│   │   │   ├── SettingsView.vue
│   │   │   ├── IconsView.vue
│   │   │   └── LoginView.vue        ← 新增
│   │   ├── composables/
│   │   │   ├── usePlaybackProgress.ts   ← 现有:定时器副作用
│   │   │   ├── useAudioEngine.ts        ← 新增:HTMLAudioElement 封装
│   │   │   ├── usePlayMode.ts           ← 新增:playMode 逻辑
│   │   │   ├── useLyricSync.ts          ← 新增:歌词同步防抖
│   │   │   └── queries/                 ← TanStack Query 包装(可选,简单场景可直接调 window.api)
│   │   │       ├── usePlaylists.ts
│   │   │       ├── useFavoriteList.ts
│   │   │       └── useLyric.ts
│   │   ├── stores/                  ← Pinia,只存 UI 状态
│   │   │   ├── auth.ts              ← B 站登录状态镜像(由主进程推送)
│   │   │   ├── player.ts            ← 当前播放镜像
│   │   │   ├── queue.ts             ← 队列(UI 侧副本)
│   │   │   ├── playback.ts          ← 播放控制状态
│   │   │   ├── playlist.ts          ← 歌单缓存(由 TanStack Query 填充)
│   │   │   ├── lyric.ts             ← 歌词缓存 + currentLyricIndex
│   │   │   └── theme.ts             ← 主题
│   │   ├── router/
│   │   │   └── index.ts             ← 补 404 路由
│   │   ├── utils/                   ← UI 工具
│   │   │   ├── icons.ts
│   │   │   ├── format.ts            ← 包含迁移来的 totalDuration
│   │   │   ├── lottie.ts
│   │   │   └── lrcParser.ts         ← 歌词解析(纯函数,与主进程共用)
│   │   ├── lib/
│   │   │   └── queryClient.ts       ← TanStack Query 配置
│   │   ├── types/                   ← 渲染进程类型(从 shared 导入或本地补充)
│   │   ├── App.vue
│   │   ├── main.ts
│   │   └── style.css
│   │
│   └── shared/                      ← 主/渲染/preload 三端共享(与三进程平级)
│       ├── ipc-channels.ts          ← IPC 通道名常量
│       ├── ipc-types.ts             ← IPC 请求/响应类型
│       └── types.ts                 ← 共享业务类型(Track / Playlist 等)
│
├── resources/                       ← 打包资源(图标等)
└── docs/
    └── plan/                        ← 计划文档
```

### 2.2 关键约定

1. **`src/main/lib/`** 是"后端逻辑层",目录名与 BBPlayer 的 `apps/mobile/src/lib/` 对齐,便于迁移时按目录对照;实际 import 走 TS path alias(`@main/*` / `@renderer/*` / `@shared/*`),文件系统路径与 import 路径解耦
2. **`src/shared/`** 是主/渲染/preload 三端共享层,放 IPC 通道名常量 + 类型定义,**与 `src/main` / `src/preload` / `src/renderer` 平级**(不嵌在任一进程目录内),避免渲染进程"穿透" main 目录
3. **`src/renderer/`(渲染进程)** 只负责 UI,不直接调任何"后端"模块,所有数据获取走 `window.api`
4. **`src/renderer/utils/lrcParser.ts`** 是纯函数,渲染进程内部使用;若主进程也要用,搬到 `src/shared/utils/lrcParser.ts`
5. **三套 tsconfig**:`tsconfig.node.json` 管 `src/main` + `src/preload`(Node 环境),`tsconfig.web.json` 管 `src/renderer`(DOM 环境),`tsconfig.json` 通过 `references` 聚合
6. **alias 映射**(在 `electron.vite.config.ts` 与各 tsconfig 中同步):
   - `@main/*` → `src/main/*`
   - `@preload/*` → `src/preload/*`
   - `@renderer/*` → `src/renderer/*`(也可继续用 `@/*` 指 `src/renderer/`,迁移时减少业务代码 import 改动)
   - `@shared/*` → `src/shared/*`

---

## 3. 技术选型

### 3.1 完整技术栈

| 维度 | 选择 | 理由 |
|---|---|---|
| Electron 集成 | **electron-vite**(alex8088) | 官方推荐脚手架,主/渲染/preload 三端 HMR 完善 |
| 主进程语言 | TypeScript + Node.js | Electron 原生支持 |
| HTTP client | **原生 `https` 模块** 或 `undici` | Node 环境,无 CORS;`undici` 是 Node 官方 fetch 实现,API 现代 |
| 数据库 | **better-sqlite3** | 同步 API,性能极强(比 sql.js 快 10 倍),Electron 生态首选 |
| ORM | **Drizzle ORM**(`drizzle-orm/better-sqlite3`) | 与 BBPlayer 完全一致,类型完整,关系查询全可用 |
| Cookie 管理 | **tough-cookie** | Node 生态成熟的 cookie jar 库,自动处理 domain/path/expires |
| 错误处理 | **neverthrow** | 与 BBPlayer 一致,`ResultAsync` 不 throw |
| 渲染进程框架 | Vue 3 + TypeScript | 现有项目 |
| 渲染进程状态 | Pinia | 现有项目,只存 UI 状态 |
| 渲染进程异步缓存 | **@tanstack/vue-query** | 与 BBPlayer 一致 |
| 渲染进程 UI | Tailwind CSS v4 + MD3 | 现有项目 |
| 打包 | **electron-builder** | 跨平台打包,社区主流 |

### 3.2 依赖清单

**根 `package.json`**:

```json
{
  "dependencies": {
    "better-sqlite3": "^11.x",
    "drizzle-orm": "^0.36.x",
    "tough-cookie": "^5.x",
    "neverthrow": "^8.x",
    "undici": "^6.x",
    "@tanstack/vue-query": "^5.x",
    "vue": "^3.5.x",
    "vue-router": "^4.x",
    "pinia": "^2.x",
    "@vueuse/core": "^11.x",
    "@iconify/vue": "^4.x",
    "vue3-lottie": "^3.x",
    "@fontsource/*": "*"
  },
  "devDependencies": {
    "electron": "^33.x",
    "electron-vite": "^2.x",
    "electron-builder": "^25.x",
    "@electron/rebuild": "^3.x",
    "@types/better-sqlite3": "^7.x",
    "@types/tough-cookie": "^4.x",
    "drizzle-kit": "^0.28.x",
    "vite": "^5.x",
    "typescript": "^5.x",
    "tailwindcss": "^4.x",
    "eslint": "^9.x",
    "typescript-eslint": "^8.x",
    "eslint-plugin-vue": "^9.x",
    "vue-tsc": "^2.x"
  }
}
```

### 3.3 关键原生模块配置

`better-sqlite3` 是原生 C++ 模块,**必须按 Electron 版本 rebuild**:

```bash
# 安装后执行
pnpm exec electron-rebuild

# 或在 package.json 加 postinstall
"scripts": {
  "postinstall": "electron-rebuild"
}
```

`electron.vite.config.ts` 中要排除 `better-sqlite3`(不打包进 main bundle,运行时从 node_modules 加载):

```ts
import { defineConfig } from 'electron-vite'
import { resolve } from 'path'

export default defineConfig({
  main: {
    build: {
      rollupOptions: {
        external: ['better-sqlite3'],
      },
    },
  },
  preload: {
    build: {
      rollupOptions: {
        input: resolve(__dirname, 'src/preload/index.ts'),
      },
    },
  },
  renderer: {
    root: 'src/renderer',
    build: {
      rollupOptions: {
        input: resolve(__dirname, 'src/renderer/index.html'),
      },
    },
    resolve: {
      alias: {
        // 见 2.2 节约定;@/* 兼容现有 Vue 代码 import 习惯
        '@': resolve(__dirname, 'src/renderer'),
        '@renderer': resolve(__dirname, 'src/renderer'),
        '@shared': resolve(__dirname, 'src/shared'),
      },
    },
  },
})
```

---

## 4. 主进程详细设计

### 4.1 入口 `src/main/index.ts`

```ts
import { app, BrowserWindow } from 'electron'
import { createWindow } from './window'
import { registerAllIpc } from './ipc'
import { appState } from './lib/config/store'
import { initDb } from './lib/db'
import { startAudioProxy } from './lib/audio/streamProxy'

// 单例锁(防止多开)
const gotTheLock = app.requestSingleInstanceLock()
if (!gotTheLock) {
  app.quit()
}

app.whenReady().then(async () => {
  // 1. 加载持久化状态(cookie 等)
  await appState.load()

  // 2. 初始化数据库
  initDb()

  // 3. 启动音频流代理 server
  await startAudioProxy()

  // 4. 注册 IPC handler
  registerAllIpc()

  // 5. 创建主窗口
  createWindow()

  app.on('activate', () => {
    if (BrowserWindow.getAllWindows().length === 0) createWindow()
  })
})

app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') app.quit()
})

// 应用退出前持久化状态
app.on('before-quit', async () => {
  await appState.persist()
})
```

### 4.2 窗口管理 `src/main/window.ts`

```ts
import { BrowserWindow, shell } from 'electron'
import { join } from 'path'
import { is } from '@electron-toolkit/utils'

export function createWindow(): BrowserWindow {
  const mainWindow = new BrowserWindow({
    width: 1200,
    height: 800,
    minWidth: 900,
    minHeight: 600,
    show: false,
    autoHideMenuBar: true,
    titleBarStyle: process.platform === 'darwin' ? 'hiddenInset' : 'default',
    webPreferences: {
      preload: join(__dirname, '../preload/index.js'),
      sandbox: false,                // 关闭 sandbox 以便 preload 用 Node 集成
      contextIsolation: true,        // 必须开,contextBridge 才能工作
      nodeIntegration: false,        // 渲染进程不直接用 Node
    },
  })

  mainWindow.on('ready-to-show', () => {
    mainWindow.show()
  })

  // 外部链接用系统浏览器打开
  mainWindow.webContents.setWindowOpenHandler(({ url }) => {
    shell.openExternal(url)
    return { action: 'deny' }
  })

  // 开发模式加载 Vite dev server,生产加载打包文件
  if (is.dev && process.env['ELECTRON_RENDERER_URL']) {
    mainWindow.loadURL(process.env['ELECTRON_RENDERER_URL'])
  } else {
    mainWindow.loadFile(join(__dirname, '../renderer/index.html'))
  }

  return mainWindow
}
```

### 4.3 状态管理 `src/main/lib/config/store.ts`

主进程不需要响应式框架,用**模块级单例变量**:

```ts
import { app } from 'electron'
import { join } from 'path'
import { readFileSync, writeFileSync, existsSync } from 'fs'
import type { BilibiliCookie, BilibiliUser } from '../../../shared/types'

interface PersistedState {
  bilibiliCookie: BilibiliCookie | null
  bilibiliUser: BilibiliUser | null
  preferences: Record<string, unknown>
}

class AppState {
  bilibiliCookie: BilibiliCookie | null = null
  bilibiliUser: BilibiliUser | null = null
  preferences: Record<string, unknown> = {}

  private get statePath(): string {
    return join(app.getPath('userData'), 'state.json')
  }

  async load(): Promise<void> {
    if (!existsSync(this.statePath)) return
    try {
      const data = JSON.parse(readFileSync(this.statePath, 'utf-8')) as PersistedState
      this.bilibiliCookie = data.bilibiliCookie
      this.bilibiliUser = data.bilibiliUser
      this.preferences = data.preferences ?? {}
    } catch (e) {
      console.error('Failed to load state:', e)
    }
  }

  async persist(): Promise<void> {
    const data: PersistedState = {
      bilibiliCookie: this.bilibiliCookie,
      bilibiliUser: this.bilibiliUser,
      preferences: this.preferences,
    }
    writeFileSync(this.statePath, JSON.stringify(data, null, 2))
  }

  setCookie(cookie: BilibiliCookie | null): void {
    this.bilibiliCookie = cookie
    this.persist()
    // 通知渲染进程
    this.notifyRenderer('auth:stateChanged', { user: this.bilibiliUser, isLoggedIn: !!cookie })
  }

  setUser(user: BilibiliUser | null): void {
    this.bilibiliUser = user
    this.persist()
    this.notifyRenderer('auth:stateChanged', { user, isLoggedIn: !!this.bilibiliCookie })
  }

  private notifyRenderer(channel: string, data: unknown): void {
    const { BrowserWindow } = require('electron')
    BrowserWindow.getAllWindows().forEach((win: import('electron').BrowserWindow) => {
      win.webContents.send(channel, data)
    })
  }
}

export const appState = new AppState()
```

### 4.4 数据库层 `src/main/lib/db/`

#### `schema.ts`(1:1 复刻 BBPlayer,改 import 路径)

```ts
import { sql } from 'drizzle-orm'
import { sqliteTable, text, integer, primaryKey, index, uniqueIndex } from 'drizzle-orm/sqlite-core'

// 用户表(B 站账号镜像,无密码)
export const users = sqliteTable('users', {
  id: text('id').primaryKey(),
  bilibiliUid: text('bilibili_uid').unique(),
  name: text('name').notNull(),
  face: text('face'),
  lastLoginAt: integer('last_login_at').mode('timestamp'),
})

// 歌单表
export const playlists = sqliteTable('playlists', {
  id: text('id').primaryKey(),
  type: text('type', { enum: ['local', 'synced', 'favorite', 'toview'] }).notNull(),
  title: text('title').notNull(),
  coverUrl: text('cover_url'),
  sourceId: text('source_id'),       // B 站收藏夹 ID / 合集 ID / UP 主 UID
  createdAt: integer('created_at').mode('timestamp').notNull().default(sql`(unixepoch())`),
  updatedAt: integer('updated_at').mode('timestamp').notNull().default(sql`(unixepoch())`),
}, (t) => ({
  typeIdx: index('playlists_type_idx').on(t.type),
  sourceIdx: index('playlists_source_idx').on(t.sourceId),
}))

// 曲目表(全局曲目池,跨歌单共享)
export const tracks = sqliteTable('tracks', {
  id: text('id').primaryKey(),
  uniqueKey: text('unique_key').unique().notNull(),
  title: text('title').notNull(),
  artistId: text('artist_id'),
  coverUrl: text('cover_url'),
  duration: integer('duration').notNull(),
  bilibiliBvid: text('bilibili_bvid'),
  bilibiliCid: text('bilibili_cid'),
})

// 艺术家表
export const artists = sqliteTable('artists', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  remoteId: text('remote_id'),
})

// 歌单-曲目关系表
export const playlistTracks = sqliteTable('playlist_tracks', {
  playlistId: text('playlist_id').notNull().references(() => playlists.id, { onDelete: 'cascade' }),
  trackId: text('track_id').notNull().references(() => tracks.id, { onDelete: 'cascade' }),
  sortKey: text('sort_key').notNull(),
  addedAt: integer('added_at').mode('timestamp').notNull().default(sql`(unixepoch())`),
}, (t) => ({
  pk: primaryKey({ columns: [t.playlistId, t.trackId] }),
  sortIdx: index('pt_sort_idx').on(t.playlistId, t.sortKey),
}))

// B 站元数据缓存(音频流 URL + 过期时间)
export const bilibiliMetadata = sqliteTable('bilibili_metadata', {
  trackId: text('track_id').primaryKey().references(() => tracks.id, { onDelete: 'cascade' }),
  bvid: text('bvid').notNull(),
  cid: text('cid').notNull(),
  audioStreamUrl: text('audio_stream_url'),
  streamExpiresAt: integer('stream_expires_at').mode('timestamp'),
})

// 播放历史
export const playHistory = sqliteTable('play_history', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  trackId: text('track_id').notNull().references(() => tracks.id, { onDelete: 'cascade' }),
  playedAt: integer('played_at').mode('timestamp').notNull().default(sql`(unixepoch())`),
}, (t) => ({
  trackIdx: index('history_track_idx').on(t.trackId),
  playedAtIdx: index('history_played_at_idx').on(t.playedAt),
}))

// 用户偏好
export const preferences = sqliteTable('preferences', {
  key: text('key').primaryKey(),
  value: text('value').notNull(),    // JSON 字符串
})

// 歌词缓存
export const lyrics = sqliteTable('lyrics', {
  trackId: text('track_id').primaryKey().references(() => tracks.id, { onDelete: 'cascade' }),
  source: text('source', { enum: ['netease', 'qqmusic', 'kugou', 'bilibili'] }).notNull(),
  lrc: text('lrc').notNull(),
  translation: text('translation'),
  fetchedAt: integer('fetched_at').mode('timestamp').notNull().default(sql`(unixepoch())`),
})
```

#### `index.ts`(数据库连接工厂)

```ts
import Database from 'better-sqlite3'
import { drizzle } from 'drizzle-orm/better-sqlite3'
import { app } from 'electron'
import { join } from 'path'
import * as schema from './schema'

let dbInstance: ReturnType<typeof drizzle<typeof schema>> | null = null
let sqliteInstance: Database.Database | null = null

export function initDb(): void {
  if (dbInstance) return

  const dbPath = join(app.getPath('userData'), 'bbplayer-desk.db')
  console.log('[db] SQLite path:', dbPath)

  sqliteInstance = new Database(dbPath)
  // 性能优化:WAL 模式 + 异步提交
  sqliteInstance.pragma('journal_mode = WAL')
  sqliteInstance.pragma('synchronous = NORMAL')

  dbInstance = drizzle(sqliteInstance, { schema })
}

export function getDb() {
  if (!dbInstance) throw new Error('DB not initialized. Call initDb() first.')
  return dbInstance
}

export function closeDb(): void {
  if (sqliteInstance) {
    sqliteInstance.close()
    sqliteInstance = null
    dbInstance = null
  }
}
```

### 4.5 B 站 API client `src/main/lib/api/clients/bilibili/`

#### `client.ts`(几乎直接复刻 BBPlayer,改 cookie 来源)

```ts
import { appState } from '../../config/store'
import { Cookie, CookieJar } from 'tough-cookie'
import { ResultAsync, ok, err } from 'neverthrow'
import { request as undiciRequest } from 'undici'

export class ApiError {
  constructor(public code: number, public message: string) {}
}

class ApiClient {
  private baseUrl = 'https://api.bilibili.com'
  private cookieJar = new CookieJar()

  /** 把 appState 中的 cookie 对象同步到 cookieJar */
  private async syncCookieJar(): Promise<void> {
    if (!appState.bilibiliCookie) return
    const { SESSDATA, bili_jct, DedeUserID } = appState.bilibiliCookie
    const cookies = [
      Cookie.parse(`SESSDATA=${SESSDATA}; Domain=.bilibili.com; Path=/`),
      Cookie.parse(`bili_jct=${bili_jct}; Domain=.bilibili.com; Path=/`),
      Cookie.parse(`DedeUserID=${DedeUserID}; Domain=.bilibili.com; Path=/`),
    ].filter(Boolean) as Cookie[]
    for (const cookie of cookies) {
      await this.cookieJar.setCookie(cookie, 'https://api.bilibili.com')
    }
  }

  async request<T>(opts: {
    endpoint?: string
    fullUrl?: string
    method?: string
    body?: unknown
    params?: Record<string, unknown>
    skipCookie?: boolean
    signal?: AbortSignal
    headers?: Record<string, string>
  }): Promise<ResultAsync<T, ApiError>> {
    return ResultAsync.fromPromise(
      (async () => {
        const url = opts.fullUrl ?? `${this.baseUrl}${opts.endpoint}`
        if (!opts.skipCookie) await this.syncCookieJar()

        const cookieHeader = opts.skipCookie
          ? undefined
          : await this.cookieJar.getCookies(url).then(cs => cs.map(c => c.cookieString()).join('; '))

        // Node 环境:User-Agent / Referer / Origin 任意设,无浏览器限制
        const defaultHeaders: Record<string, string> = {
          'User-Agent': 'Mozilla/5.0 (iPhone; CPU iPhone OS 14_0_1 like Mac OS X) AppleWebKit/605.1.15 BiliApp/6.66.0',
          Referer: 'https://www.bilibili.com/',
          Origin: 'https://www.bilibili.com',
          ...(cookieHeader ? { Cookie: cookieHeader } : {}),
          ...(opts.headers ?? {}),
        }

        const response = await undiciRequest(url, {
          method: opts.method ?? 'GET',
          headers: defaultHeaders,
          body: opts.body ? JSON.stringify(opts.body) : undefined,
          signal: opts.signal,
        })

        // Node 环境:Set-Cookie 响应头完全可读
        const setCookies = response.headers['set-cookie']
        if (setCookies && !opts.skipCookie) {
          for (const c of setCookies) {
            await this.cookieJar.setCookie(c, url)
          }
        }

        if (response.statusCode >= 400) {
          throw new ApiError(response.statusCode, `HTTP ${response.statusCode}`)
        }

        return (await response.body.json()) as T
      })(),
      (e): ApiError => {
        if (e instanceof ApiError) return e
        return new ApiError(-1, e instanceof Error ? e.message : String(e))
      },
    )
  }
}

export const bilibiliApiClient = new ApiClient()
```

**与 BBPlayer 的差异**:
- BBPlayer 用 `fetch` + RN 自动 cookie jar;Electron 用 `undici` + `tough-cookie` 手动管理
- BBPlayer 从 `useAppStore.getState().bilibiliCookie` 读 cookie;Electron 从 `appState.bilibiliCookie` 读
- 其余 100% 一致(包括伪装成 B 站 web 端的 UA / Referer / Origin)

#### `wbi.ts`(完全复刻 BBPlayer)

```ts
import { createHash } from 'crypto'
import { localStorageLike } from '../utils/storage'  // 用 electron-store 或自定义实现

const mixinKeyEncTab = [
  46, 47, 18, 2, 53, 8, 23, 32, 15, 50, 10, 31, 58, 3, 45, 35,
  27, 43, 5, 49, 33, 9, 42, 19, 29, 28, 14, 39, 12, 38, 41, 13,
  37, 36, 25, 1, 51, 0, 24, 56, 55, 61, 4, 6, 17, 20, 62, 30,
  48, 59, 22, 44, 16, 40, 52, 7, 34, 21, 63, 11, 57, 60, 26, 54,
]

export function getMixinKey(orig: string): string {
  return mixinKeyEncTab.slice(0, 32).map((k) => orig[k]).join('')
}

export function encWbi(
  params: Record<string, unknown>,
  imgKey: string,
  subKey: string,
): Record<string, string> {
  const mixinKey = getMixinKey(imgKey + subKey)
  const wts = Math.floor(Date.now() / 1000)
  const wbiSignParams = { wts, ...params }
  // 按 key 字典序排序
  const sortedQuery = Object.keys(wbiSignParams)
    .sort()
    .map((k) => `${k}=${wbiSignParams[k]}`)
    .join('&')
  // Node 原生 crypto,无需第三方 md5
  const w_rid = createHash('md5').update(sortedQuery + mixinKey).digest('hex')
  return { wts: String(wts), w_rid }
}

/** 缓存 wbi_keys,每日刷新一次 */
export async function getWbiKeys(): Promise<{ imgKey: string; subKey: string }> {
  // 1. 先读缓存(localStorage-like)
  const cached = await localStorageLike.get('wbi_keys')
  if (cached && Date.now() - cached.fetchedAt < 24 * 60 * 60 * 1000) {
    return { imgKey: cached.imgKey, subKey: cached.subKey }
  }
  // 2. 缓存失效,调 /x/web-interface/nav 拉新
  const result = await bilibiliApiClient.request<{ data: { wbi_img: { img_url: string; sub_url: string } } }>({
    endpoint: '/x/web-interface/nav',
  })
  if (!result.isOk()) throw new Error('Failed to fetch wbi_keys')
  const { img_url, sub_url } = result.value.data.wbi_img
  const imgKey = img_url.split('/').pop()!.split('.')[0]
  const subKey = sub_url.split('/').pop()!.split('.')[0]
  await localStorageLike.set('wbi_keys', { imgKey, subKey, fetchedAt: Date.now() })
  return { imgKey, subKey }
}
```

**与 BBPlayer 的差异**:
- BBPlayer 用 RN 的 `MMKV` 缓存 wbi_keys;Electron 用 `electron-store` 或主进程的 JSON 文件
- BBPlayer 用第三方 md5 库;Electron 用 Node 原生 `crypto` 模块,**无需依赖**

#### `api.ts`(1:1 复刻 BBPlayer 接口封装)

```ts
import { bilibiliApiClient } from './client'
import { encWbi, getWbiKeys } from './wbi'

export class BilibiliApi {
  /** 扫码登录 - 获取二维码 */
  getLoginQrCode() {
    return bilibiliApiClient.request<BilibiliQrCodeResponse>({
      endpoint: '/x/passport-tv-login/qrcode/auth_code',
      method: 'GET',
      skipCookie: true,
    })
  }

  /** 扫码登录 - 轮询登录状态 */
  pollQrCodeLoginStatus(qrcodeKey: string) {
    return bilibiliApiClient.request<BilibiliLoginStatusResponse>({
      endpoint: '/x/passport-tv-login/qrcode/poll',
      method: 'GET',
      params: { auth_code: qrcodeKey },
      skipCookie: true,
    })
  }

  /** 用户信息 */
  getUserInfo() {
    return bilibiliApiClient.request<BilibiliUserResponse>({
      endpoint: '/x/web-interface/nav',
    })
  }

  /** 收藏夹列表 */
  getFavoritePlaylists() {
    return bilibiliApiClient.request<BilibiliFavoritePlaylistsResponse>({
      endpoint: '/x/v3/fav/folder/created/list-all',
    })
  }

  /** 收藏夹内容(分页) */
  getFavoriteListContents(favoriteId: number, pn: number = 1) {
    return bilibiliApiClient.request<BilibiliFavoriteListContentsResponse>({
      endpoint: '/x/v3/fav/resource/list',
      method: 'GET',
      params: { media_id: favoriteId, pn, ps: 20 },
    })
  }

  /** 视频信息(含多 P) */
  getVideoInfo(bvid: string) {
    return bilibiliApiClient.request<BilibiliVideoInfoResponse>({
      endpoint: '/x/web-interface/view',
      method: 'GET',
      params: { bvid },
    })
  }

  /** 视频转音频流 URL(dolby > hi-res > normal) */
  async getAudioStream(bvid: string, cid: string): Promise<ResultAsync<string, ApiError>> {
    // 调 /x/player/playurl,优先级 dolby > hi-res > normal
    // 完整实现参考 BBPlayer api.ts 第 401-528 行
    // 1. 获取 playurl
    // 2. 检查 dolby 音轨
    // 3. 检查 hi-res 音轨
    // 4. 降级到 normal
    // 5. 返回最佳音质 URL
  }

  /** 搜索视频(WBI 签名) */
  async searchVideos(keyword: string, page: number = 1) {
    const { imgKey, subKey } = await getWbiKeys()
    const params = { keyword, search_type: 'video', page }
    const signedParams = encWbi(params, imgKey, subKey)
    return bilibiliApiClient.request<BilibiliSearchResponse>({
      endpoint: '/x/wbi/search/type',
      method: 'GET',
      params: signedParams,
    })
  }

  // ... 其余 30+ 接口,按 BBPlayer api.ts 1:1 复刻
}

export const bilibiliApi = new BilibiliApi()
```

### 4.6 service 层(1:1 复刻 BBPlayer)

```ts
// src/main/lib/services/playlistService.ts
import { getDb } from '../db'
import { playlists, playlistTracks, tracks, artists } from '../db/schema'
import { eq, and, asc } from 'drizzle-orm'
import type { Playlist } from '../../../shared/types'

export class PlaylistService {
  async getAll(): Promise<Playlist[]> {
    const db = getDb()
    const rows = await db.query.playlists.findMany({
      with: {
        tracks: {
          with: {
            track: {
              with: {
                artist: true,
              },
            },
          },
          orderBy: [asc(playlistTracks.sortKey)],
        },
      },
      orderBy: [asc(playlists.createdAt)],
    })
    return rows.map(transformRowToPlaylist)
  }

  async getById(id: string): Promise<Playlist | null> {
    const db = getDb()
    const row = await db.query.playlists.findFirst({
      where: eq(playlists.id, id),
      with: {
        tracks: {
          with: {
            track: {
              with: {
                artist: true,
              },
            },
          },
          orderBy: [asc(playlistTracks.sortKey)],
        },
      },
    })
    return row ? transformRowToPlaylist(row) : null
  }

  async create(playlist: Playlist): Promise<void> {
    const db = getDb()
    await db.insert(playlists).values({
      id: playlist.id,
      type: playlist.type,
      title: playlist.title,
      coverUrl: playlist.coverUrl,
      sourceId: playlist.sourceId,
    })
  }

  async delete(id: string): Promise<void> {
    const db = getDb()
    await db.delete(playlists).where(eq(playlists.id, id))
    // playlistTracks 因外键 cascade 自动删除
  }

  // ... update / addTrack / removeTrack / reorder 等
}

export const playlistService = new PlaylistService()
```

**关键**:与 BBPlayer 的 `playlistService.ts` **几乎逐行一致**,因为都是 Drizzle + SQLite + 关系查询。这是直接上 Electron 的最大收益。

### 4.7 facade 层(1:1 复刻 BBPlayer)

```ts
// src/main/lib/facades/bilibiliPlaylist.ts
import { bilibiliApi } from '../api/clients/bilibili'
import { playlistService } from '../services/playlistService'
import { trackService } from '../services/trackService'
import { getDb } from '../db'
import { playlists, tracks, playlistTracks } from '../db/schema'
import { eq } from 'drizzle-orm'

export class BilibiliPlaylistFacade {
  /** 收藏夹同步到本地 */
  async syncFavoriteList(favoriteId: number): Promise<void> {
    let pn = 1
    const allTracks: Track[] = []

    // 1. 分页拉取收藏夹内容(网络在事务外)
    while (true) {
      const result = await bilibiliApi.getFavoriteListContents(favoriteId, pn)
      if (!result.isOk()) break

      const { medias, has_more } = result.value.data
      if (!medias?.length) break

      for (const media of medias) {
        allTracks.push(transformBilibiliFavToTrack(media))
      }

      if (!has_more) break
      pn++
    }

    // 2. 事务写入本地 SQLite(事务内不调网络)
    const db = getDb()
    await db.transaction(async (tx) => {
      const playlistId = `favorite-${favoriteId}`
      await tx.insert(playlists)
        .values({ id: playlistId, type: 'favorite', title: `B站收藏夹 ${favoriteId}`, sourceId: String(favoriteId) })
        .onConflictDoUpdate({
          target: playlists.id,
          set: { title: `B站收藏夹 ${favoriteId}` },
        })

      for (const track of allTracks) {
        await tx.insert(tracks)
          .values(track)
          .onConflictDoUpdate({
            target: tracks.id,
            set: { title: track.title, coverUrl: track.coverUrl, duration: track.duration },
          })

        await tx.insert(playlistTracks)
          .values({ playlistId, trackId: track.id, sortKey: generateSortKey() })
          .onConflictDoNothing()
      }
    })
  }
}

export const bilibiliPlaylistFacade = new BilibiliPlaylistFacade()
```

**关键**:与 BBPlayer 的 `syncBilibiliPlaylist.ts` 几乎逐行一致,事务内嵌 SQL、`onConflictDoUpdate` 等 Drizzle 特性都能用。

### 4.8 音频流本地代理 `src/main/lib/audio/streamProxy.ts`

B 站音频流 URL 在主进程解析,但音频要在渲染进程的 `<audio>` 标签里播放。如果直接把 B 站 CDN URL 传给渲染进程,Chromium 不会带 B 站 cookie/Referer,会被 403。

**解决方案**:主进程起一个本地 HTTP server,把 B 站音频流转发到 `http://localhost:xxx/stream?url=...`,渲染进程的 `<audio>` 走本地 URL。

```ts
import { createServer, IncomingMessage, ServerResponse } from 'http'
import { request as undiciRequest } from 'undici'
import { appState } from '../config/store'

let server: ReturnType<typeof createServer> | null = null
let proxyPort: number | null = null

export async function startAudioProxy(): Promise<void> {
  if (server) return

  server = createServer(async (req: IncomingMessage, res: ServerResponse) => {
    if (!req.url?.startsWith('/stream')) {
      res.writeHead(404)
      res.end('Not Found')
      return
    }

    const url = new URL(req.url, `http://localhost:${proxyPort}`)
    const targetUrl = url.searchParams.get('url')
    if (!targetUrl) {
      res.writeHead(400)
      res.end('Missing url param')
      return
    }

    try {
      // 主进程带 B 站 cookie / Referer / UA 请求音频流
      const cookieStr = appState.bilibiliCookie
        ? `SESSDATA=${appState.bilibiliCookie.SESSDATA}`
        : ''

      const upstream = await undiciRequest(targetUrl, {
        method: 'GET',
        headers: {
          Cookie: cookieStr,
          'User-Agent': 'Mozilla/5.0 (iPhone; CPU iPhone OS 14_0_1 like Mac OS X) AppleWebKit/605.1.15 BiliApp/6.66.0',
          Referer: 'https://www.bilibili.com/',
          Origin: 'https://www.bilibili.com',
        },
      })

      if (upstream.statusCode !== 200) {
        res.writeHead(upstream.statusCode)
        res.end(`Upstream error: ${upstream.statusCode}`)
        return
      }

      // 转发响应头(Content-Type / Content-Length / Range 支持)
      const headers: Record<string, string> = {}
      for (const [k, v] of Object.entries(upstream.headers)) {
        if (typeof v === 'string') headers[k] = v
      }
      res.writeHead(200, headers)

      // 流式转发
      upstream.body.pipe(res)
    } catch (e) {
      res.writeHead(500)
      res.end(`Proxy error: ${e instanceof Error ? e.message : String(e)}`)
    }
  })

  // 随机端口启动
  await new Promise<void>((resolve) => {
    server!.listen(0, '127.0.0.1', () => {
      const addr = server!.address()
      if (addr && typeof addr === 'object') {
        proxyPort = addr.port
        console.log(`[audio-proxy] Listening on http://127.0.0.1:${proxyPort}`)
      }
      resolve()
    })
  })
}

export function getProxyPort(): number {
  if (!proxyPort) throw new Error('Audio proxy not started')
  return proxyPort
}

export function stopAudioProxy(): void {
  if (server) {
    server.close()
    server = null
    proxyPort = null
  }
}
```

**使用**:主进程解析 B 站音频流 URL 后,包装成本地代理 URL 返回给渲染进程:

```ts
// src/main/lib/facades/playback.ts
import { bilibiliApi } from '../api/clients/bilibili'
import { getProxyPort } from '../audio/streamProxy'

export class PlaybackFacade {
  async getAudioUrl(trackId: string): Promise<string> {
    // 1. 从 SQLite 查 bvid + cid
    const meta = await bilibiliMetadataService.getByTrackId(trackId)

    // 2. 检查缓存的音频流 URL 是否过期
    if (meta?.audioStreamUrl && meta.streamExpiresAt && meta.streamExpiresAt > new Date()) {
      return wrapWithProxy(meta.audioStreamUrl)
    }

    // 3. 调 B 站 API 拉新 URL
    const result = await bilibiliApi.getAudioStream(meta!.bvid, meta!.cid)
    if (!result.isOk()) throw new Error('Failed to get audio stream')
    const streamUrl = result.value

    // 4. 缓存到 SQLite
    await bilibiliMetadataService.updateStreamUrl(trackId, streamUrl, new Date(Date.now() + 2 * 60 * 60 * 1000))

    // 5. 包装成本地代理 URL
    return wrapWithProxy(streamUrl)
  }
}

function wrapWithProxy(url: string): string {
  return `http://127.0.0.1:${getProxyPort()}/stream?url=${encodeURIComponent(url)}`
}
```

渲染进程:

```ts
// 渲染进程
const audioUrl = await window.api.playback.getAudioUrl(track.id)
audioElement.src = audioUrl
audioElement.play()
```

---

## 5. IPC 通信设计

### 5.1 通道命名约定

格式:`<module>:<action>`,例:`auth:login`、`playlist:getAll`、`bilibili:syncFavoriteList`。

通道名集中定义,避免字符串硬编码:

```ts
// src/shared/ipc-channels.ts
export const IPC_CHANNELS = {
  AUTH: {
    GET_QR_CODE: 'auth:getQrCode',
    POLL_QR_LOGIN: 'auth:pollQrLogin',
    SET_COOKIE: 'auth:setCookie',
    GET_USER_INFO: 'auth:getUserInfo',
    LOGOUT: 'auth:logout',
    STATE_CHANGED: 'auth:stateChanged',  // 主进程 → 渲染进程
  },
  PLAYLIST: {
    GET_ALL: 'playlist:getAll',
    GET_BY_ID: 'playlist:getById',
    CREATE: 'playlist:create',
    DELETE: 'playlist:delete',
    SYNC_FAVORITE: 'playlist:syncFavorite',
  },
  TRACK: {
    GET_BY_ID: 'track:getById',
    SEARCH: 'track:search',
  },
  BILIBILI: {
    GET_FAVORITE_LISTS: 'bilibili:getFavoriteLists',
    SEARCH: 'bilibili:search',
  },
  LYRIC: {
    GET: 'lyric:get',
  },
  PLAYBACK: {
    GET_AUDIO_URL: 'playback:getAudioUrl',
  },
  HISTORY: {
    RECORD: 'history:record',
    GET_RECENT: 'history:getRecent',
  },
} as const
```

### 5.2 IPC handler 注册

```ts
// src/main/ipc/index.ts
import { ipcMain } from 'electron'
import { registerAuthIpc } from './auth'
import { registerPlaylistIpc } from './playlist'
import { registerBilibiliIpc } from './bilibili'
import { registerLyricIpc } from './lyric'
import { registerPlaybackIpc } from './playback'
import { registerHistoryIpc } from './history'

export function registerAllIpc(): void {
  registerAuthIpc()
  registerPlaylistIpc()
  registerBilibiliIpc()
  registerLyricIpc()
  registerPlaybackIpc()
  registerHistoryIpc()

  console.log('[ipc] All handlers registered')
}
```

```ts
// src/main/ipc/auth.ts
import { ipcMain } from 'electron'
import { IPC_CHANNELS } from '../../shared/ipc-channels'
import { bilibiliApi } from '../lib/api/clients/bilibili'
import { appState } from '../lib/config/store'
import { parseCookieFromLoginResponse } from '../lib/auth/qrLogin'

export function registerAuthIpc(): void {
  // 扫码登录 - 获取二维码
  ipcMain.handle(IPC_CHANNELS.AUTH.GET_QR_CODE, async () => {
    const result = await bilibiliApi.getLoginQrCode()
    return result.match(
      (data) => ({ success: true, data }),
      (err) => ({ success: false, error: err.message }),
    )
  })

  // 扫码登录 - 轮询登录状态
  ipcMain.handle(IPC_CHANNELS.AUTH.POLL_QR_LOGIN, async (_, qrcodeKey: string) => {
    const result = await bilibiliApi.pollQrCodeLoginStatus(qrcodeKey)
    return result.match(
      async (data) => {
        if (data.code === 0) {
          // 登录成功,从 response 解析 cookie
          const cookie = parseCookieFromLoginResponse(data)
          appState.setCookie(cookie)
          // 拉取用户信息
          const userResult = await bilibiliApi.getUserInfo()
          if (userResult.isOk()) {
            appState.setUser(userResult.value.data)
          }
          return { success: true, data: { user: appState.bilibiliUser } }
        }
        return { success: false, code: data.code, message: data.message }
      },
      (err) => ({ success: false, error: err.message }),
    )
  })

  // 手动设置 cookie
  ipcMain.handle(IPC_CHANNELS.AUTH.SET_COOKIE, async (_, cookieStr: string) => {
    try {
      const cookie = parseCookieString(cookieStr)
      appState.setCookie(cookie)
      const userResult = await bilibiliApi.getUserInfo()
      if (userResult.isOk()) {
        appState.setUser(userResult.value.data)
      }
      return { success: true }
    } catch (e) {
      return { success: false, error: e instanceof Error ? e.message : String(e) }
    }
  })

  // 获取当前用户信息
  ipcMain.handle(IPC_CHANNELS.AUTH.GET_USER_INFO, async () => {
    return { user: appState.bilibiliUser, isLoggedIn: !!appState.bilibiliCookie }
  })

  // 登出
  ipcMain.handle(IPC_CHANNELS.AUTH.LOGOUT, async () => {
    appState.setCookie(null)
    appState.setUser(null)
    return { success: true }
  })
}
```

### 5.3 preload `contextBridge`

```ts
// src/preload/index.ts
import { contextBridge, ipcRenderer } from 'electron'
import { IPC_CHANNELS } from '../shared/ipc-channels'

const api = {
  auth: {
    getQrCode: () => ipcRenderer.invoke(IPC_CHANNELS.AUTH.GET_QR_CODE),
    pollQrLogin: (qrcodeKey: string) => ipcRenderer.invoke(IPC_CHANNELS.AUTH.POLL_QR_LOGIN, qrcodeKey),
    setCookie: (cookieStr: string) => ipcRenderer.invoke(IPC_CHANNELS.AUTH.SET_COOKIE, cookieStr),
    getUserInfo: () => ipcRenderer.invoke(IPC_CHANNELS.AUTH.GET_USER_INFO),
    logout: () => ipcRenderer.invoke(IPC_CHANNELS.AUTH.LOGOUT),
    onStateChanged: (callback: (data: { user: unknown; isLoggedIn: boolean }) => void) => {
      const handler = (_: unknown, data: { user: unknown; isLoggedIn: boolean }) => callback(data)
      ipcRenderer.on(IPC_CHANNELS.AUTH.STATE_CHANGED, handler)
      // 返回取消订阅函数
      return () => ipcRenderer.removeListener(IPC_CHANNELS.AUTH.STATE_CHANGED, handler)
    },
  },
  playlist: {
    getAll: () => ipcRenderer.invoke(IPC_CHANNELS.PLAYLIST.GET_ALL),
    getById: (id: string) => ipcRenderer.invoke(IPC_CHANNELS.PLAYLIST.GET_BY_ID, id),
    create: (data: unknown) => ipcRenderer.invoke(IPC_CHANNELS.PLAYLIST.CREATE, data),
    delete: (id: string) => ipcRenderer.invoke(IPC_CHANNELS.PLAYLIST.DELETE, id),
    syncFavorite: (favoriteId: number) => ipcRenderer.invoke(IPC_CHANNELS.PLAYLIST.SYNC_FAVORITE, favoriteId),
  },
  bilibili: {
    getFavoriteLists: () => ipcRenderer.invoke(IPC_CHANNELS.BILIBILI.GET_FAVORITE_LISTS),
    search: (keyword: string, page?: number) => ipcRenderer.invoke(IPC_CHANNELS.BILIBILI.SEARCH, keyword, page),
  },
  lyric: {
    get: (trackId: string) => ipcRenderer.invoke(IPC_CHANNELS.LYRIC.GET, trackId),
  },
  playback: {
    getAudioUrl: (trackId: string) => ipcRenderer.invoke(IPC_CHANNELS.PLAYBACK.GET_AUDIO_URL, trackId),
  },
  history: {
    record: (trackId: string) => ipcRenderer.invoke(IPC_CHANNELS.HISTORY.RECORD, trackId),
    getRecent: (limit?: number) => ipcRenderer.invoke(IPC_CHANNELS.HISTORY.GET_RECENT, limit),
  },
}

contextBridge.exposeInMainWorld('api', api)

// 类型导出(渲染进程用)
export type Api = typeof api
```

### 5.4 渲染进程类型声明

```ts
// src/renderer/types/ipc.d.ts
import type { Api } from '../../preload'

declare global {
  interface Window {
    api: Api
  }
}

export {}
```

渲染进程调用:

```ts
// 任意 Vue 组件
const user = await window.api.auth.getUserInfo()
const playlists = await window.api.playlist.getAll()
await window.api.playlist.syncFavorite(12345)
```

### 5.5 状态变更通知

主进程状态变化(如扫码登录成功)主动推送给渲染进程:

```ts
// 渲染进程 - auth store
import { defineStore } from 'pinia'
import { ref, onMounted, onUnmounted } from 'vue'

export const useAuthStore = defineStore('auth', () => {
  const user = ref<BilibiliUser | null>(null)
  const isLoggedIn = ref(false)

  let unsubscribe: (() => void) | null = null

  onMounted(async () => {
    // 1. 初始加载
    const state = await window.api.auth.getUserInfo()
    user.value = state.user as BilibiliUser | null
    isLoggedIn.value = state.isLoggedIn

    // 2. 订阅主进程状态变更
    unsubscribe = window.api.auth.onStateChanged((data) => {
      user.value = data.user as BilibiliUser | null
      isLoggedIn.value = data.isLoggedIn
    })
  })

  onUnmounted(() => {
    unsubscribe?.()
  })

  return { user, isLoggedIn }
})
```

---

## 6. 鉴权管理(B 站 cookie)

### 6.1 三种登录方式

与 BBPlayer 一致:

1. **扫码登录**(`getLoginQrCode` + `pollQrCodeLoginStatus`)
   - 主进程调 B 站 passport API,返回二维码
   - 渲染进程渲染二维码,每 2 秒轮询主进程
   - 主进程调 B 站 poll 接口,从 response body 的 url 字段解析 cookie(B 站把 cookie 放 url query 里)
   - 登录成功后:`appState.setCookie(cookie)` + `appState.setUser(userInfo)`
2. **短信验证码登录**(`sendPhoneLoginSms` + `loginWithPhoneSmsCode`)
3. **手动设置 cookie**(`setCookie`)
   - 渲染进程弹 Modal,用户粘贴 cookie 字符串
   - 主进程解析,存到 `appState`

### 6.2 Cookie 持久化

- `appState.bilibiliCookie` 是普通 JS 对象(`{ SESSDATA, bili_jct, DedeUserID }`)
- 持久化到 `app.getPath('userData')/state.json`(JSON 文件)
- 启动时 `appState.load()` 读取
- 退出时 `app.on('before-quit')` 写入(已经在 `setCookie` 时即时写入,这是双保险)

### 6.3 Cookie 同步给 API client

`bilibiliApiClient` 在每次请求前调 `syncCookieJar()`,把 `appState.bilibiliCookie` 同步到 `tough-cookie` 的 `CookieJar`。这是 BBPlayer 在 RN 中用 `useAppStore.getState().bilibiliCookie` 的等价实现。

### 6.4 登录失效处理

API 返回 `-101`(未登录)时:
- 主进程 `appState.setCookie(null)` + `appState.setUser(null)`
- 推送 `auth:stateChanged` 给渲染进程
- 渲染进程弹登录 Modal

---

## 7. 渲染进程架构

### 7.1 状态管理(Pinia,只存 UI 状态)

| Store | 职责 | 数据来源 |
|---|---|---|
| `auth` | 登录状态镜像 | `window.api.auth` + 主进程推送 |
| `player` | 当前播放镜像 | `playback` store 写入 |
| `queue` | 队列 UI 副本 | `window.api.playlist.getQueue` + 本地操作 |
| `playback` | 播放控制状态(isPlaying/currentTime/volume) | UI 操作 + `<audio>` 事件 |
| `playlist` | 歌单缓存 | TanStack Query 填充 |
| `lyric` | 歌词缓存 + currentLyricIndex | `window.api.lyric.get` + 二分查找 |
| `theme` | 主题 | localStorage |

### 7.2 异步缓存(TanStack Query)

```ts
// src/renderer/lib/queryClient.ts
import { QueryClient } from '@tanstack/vue-query'

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      retry: 2,
      staleTime: 5 * 60 * 1000,   // 5 分钟
      refetchOnWindowFocus: false,
    },
    mutations: {
      retry: 0,
    },
  },
})
```

```ts
// src/renderer/composables/queries/usePlaylists.ts
import { useQuery, useMutation, useQueryClient } from '@tanstack/vue-query'

export function usePlaylists() {
  return useQuery({
    queryKey: ['playlists'],
    queryFn: () => window.api.playlist.getAll(),
  })
}

export function useSyncFavorite() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: (favoriteId: number) => window.api.playlist.syncFavorite(favoriteId),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['playlists'] })
    },
  })
}
```

### 7.3 音频播放(`useAudioEngine` composable)

```ts
// src/renderer/composables/useAudioEngine.ts
import { ref, watch } from 'vue'
import { usePlaybackStore } from '@/stores/playback'
import { usePlayerStore } from '@/stores/player'

export function useAudioEngine() {
  const audio = new Audio()
  const playbackStore = usePlaybackStore()
  const playerStore = usePlayerStore()

  // 当前曲目变化时,拉音频流 URL 并播放
  watch(() => playerStore.currentTrack, async (track) => {
    if (!track) return
    const url = await window.api.playback.getAudioUrl(track.id)
    audio.src = url
    await audio.play()
    playbackStore.isPlaying = true
  })

  // isPlaying 变化时,控制音频
  watch(() => playbackStore.isPlaying, (isPlaying) => {
    if (isPlaying) audio.play()
    else audio.pause()
  })

  // 音频时间更新
  audio.addEventListener('timeupdate', () => {
    playbackStore.currentTime = audio.currentTime
  })

  // 播放结束
  audio.addEventListener('ended', () => {
    playbackStore.next()
  })

  return { audio }
}
```

### 7.4 渲染进程与主进程的职责边界

| 场景 | 渲染进程 | 主进程 |
|---|---|---|
| 显示歌单列表 | TanStack Query 调 `window.api.playlist.getAll` | `playlistService.getAll()` 查 SQLite |
| 同步收藏夹 | 触发 mutation | `bilibiliPlaylistFacade.syncFavoriteList()` 拉 B 站 + 写 SQLite |
| 播放曲目 | `useAudioEngine` 拉 URL + `<audio>` 播放 | `playbackFacade.getAudioUrl()` 拉 B 站音频流 URL + 本地代理 |
| 切歌 | `playback.next()` 更新 player store | (不参与) |
| 歌词显示 | `lyric` store + `LyricView` | `lyricService.get()` 多源匹配 |
| 用户登录 | QR Modal + `window.api.auth.pollQrLogin` | `bilibiliApi.pollQrCodeLoginStatus` + cookie jar |
| 状态持久化 | (不参与) | `appState` 写 `state.json` |

---

## 8. 启动步骤

### 8.1 阶段 1:脚手架搭建 ✅ 已完成

**任务**(已完成):
1. electron-vite 三端构建配置(main / preload / renderer / shared)
2. 现有 Vue 代码迁移到 `src/renderer/`
3. tsconfig 拆分(node / web)
4. 主进程入口 + BrowserWindow 创建
5. preload / shared 占位

**验收标准**(已达成):
- Electron 窗口启动,显示现有 Vue UI
- 主/渲染/preload 三端 HMR 正常
- `pnpm typecheck` 通过

### 8.2 阶段 2:B 站 API client 基础(不接 DB)

**目标**:主进程能调通 B 站 API,为登录做准备。此阶段**不碰数据库**,cookie/用户信息先用主进程模块级单例 + JSON 文件持久化。

**任务**:
1. 安装 `tough-cookie` + `undici` + `neverthrow`
2. 创建 `src/main/lib/config/store.ts`(`appState` 单例,cookie/user 存内存,`state.json` 持久化到 `userData`)
3. 创建 `src/main/lib/api/clients/bilibili/client.ts`(undici + tough-cookie cookie jar + UA/Referer 注入)
4. 创建 `src/main/lib/api/clients/bilibili/wbi.ts`(WBI 签名,用 Node `crypto` 替代第三方 md5)
5. 创建 `src/main/lib/api/clients/bilibili/utils.ts`(`bv2av` / `av2bv` / CSRF 提取)
6. 创建 `src/main/lib/api/clients/bilibili/api.ts`,先实现 P0 接口:
   - `getLoginQrCode` / `pollQrCodeLoginStatus`(登录)
   - `getUserInfo`(nav 接口,验证 cookie 有效性)
   - `getFavoritePlaylists`(收藏夹列表)
   - `getFavoriteListContents`(收藏夹内容,分页)
   - `getVideoInfo`(视频信息,拿 cid)
   - `getAudioStream`(音频流 URL,dolby > hi-res > normal)
   - `searchVideos`(WBI 签名验证)
7. 主进程入口 `index.ts` 接入 `appState.load()` + `before-quit` 持久化

**验收标准**:
- 在主进程单独跑 `bilibiliApi.getUserInfo()` 能返回数据(手动塞 cookie 测试)
- WBI 签名算法能通过(`searchVideos` 不报 `-403` 鉴权错误)
- cookie jar 自动注入 `Cookie` header
- `appState` 写入 `state.json`,重启后能读回

### 8.3 阶段 3:登录流程打通(真实账号)

**目标**:用户能用真实 B 站账号登录,登录态跨重启保持。这是"一路打通"的起点。

**任务**:
1. 创建 `src/shared/ipc-channels.ts`(auth 通道名常量)
2. 创建 `src/shared/types.ts`(`BilibiliCookie` / `BilibiliUser` 类型)
3. 创建 `src/main/lib/auth/qrLogin.ts`(扫码登录轮询逻辑,从 poll response 解析 cookie)
4. 创建 `src/main/ipc/auth.ts`(登录 IPC handler:getQrCode / pollQrLogin / setCookie / getUserInfo / logout)
5. `appState.setCookie` / `setUser` 时通过 `webContents.send('auth:stateChanged')` 主动推渲染进程
6. 创建 `src/preload/index.ts`(`contextBridge.exposeInMainWorld('api', { auth: {...} })`)
7. 创建 `src/renderer/types/ipc.d.ts`(Window.api 类型声明)
8. 创建 `src/renderer/stores/auth.ts`(登录状态镜像 + 订阅主进程推送)
9. 创建 `src/renderer/components/auth/QrLoginModal.vue`(二维码 UI,2 秒轮询)
10. 创建 `src/renderer/components/auth/CookieLoginModal.vue`(手动 cookie 粘贴)
11. 创建 `src/renderer/views/LoginView.vue`(登录页,两种方式切换)
12. 路由加 `/login` + 登录守卫(未登录跳 `/login`)
13. SettingsView 显示当前用户信息(头像/昵称)+ 登出按钮

**验收标准**:
- 扫码登录成功后,用户信息显示在 SettingsView
- 手动粘贴 cookie 也能登录
- 关闭应用重新打开,登录状态保持
- 登出后清空 cookie,跳回登录页
- cookie 失效时(`-101`)自动登出并弹登录页

### 8.4 阶段 4:收藏夹拉取与展示(数据先放内存)

**目标**:登录后能看到自己 B 站收藏夹列表,点进去能看到收藏的视频(曲目)。此阶段**数据不入库**,收藏夹内容暂存 facade 内部 `Map` 缓存,够 UI 展示即可。

**任务**:
1. `src/shared/ipc-channels.ts` 补 `BILIBILI` / `PLAYLIST` 通道
2. 创建 `src/main/lib/facades/bilibiliPlaylist.ts`:
   - `getFavoriteLists()` → 调 `bilibiliApi.getFavoritePlaylists()`,内存缓存
   - `getFavoriteListContents(favoriteId)` → 分页拉取,内存缓存(按 favoriteId 存)
   - 暂不做落库,不做增量同步,全量拉取
3. 创建 `src/main/ipc/bilibili.ts`(收藏夹列表 + 内容 handler)
4. `src/preload/index.ts` 补 `bilibili` API
5. 引入 `@tanstack/vue-query`,配置 `src/renderer/lib/queryClient.ts`
6. 创建 `src/renderer/composables/queries/useFavoriteLists.ts` / `useFavoriteListContents.ts`
7. `LibraryView` 改为调 `window.api.bilibili.getFavoriteLists()`(替代假数据 `data/playlists.ts`)
8. `PlaylistView` 改为调 `window.api.bilibili.getFavoriteListContents(id)`
9. 类型适配:B 站收藏夹响应 → 现有 `Playlist` / `Track` 类型(临时映射,字段不全的留空)

**验收标准**:
- 登录后 LibraryView 显示真实 B 站收藏夹
- 点击收藏夹能看到里面的视频列表
- TanStack Query 缓存生效(切走再回来不重新请求)
- 删除 `src/renderer/data/playlists.ts` 的引用(文件可暂留,后续清理)

### 8.5 阶段 5:音频播放打通

**目标**:点击收藏夹里的视频能播放出声音。整条链路打通的最后一环。

**任务**:
1. 创建 `src/main/lib/audio/streamProxy.ts`(本地 HTTP server,127.0.0.1 随机端口,带 B 站 cookie/Referer 转发音频流)
2. 创建 `src/main/lib/facades/playback.ts`:
   - `getAudioUrl(track)` → 拿 bvid/cid → 调 `getAudioStream` → 包装成 `http://127.0.0.1:port/stream?url=...`
   - 音频流 URL 内存缓存(2 小时过期)
3. `src/shared/ipc-channels.ts` 补 `PLAYBACK` 通道
4. 创建 `src/main/ipc/playback.ts` handler
5. `src/preload/index.ts` 补 `playback.getAudioUrl`
6. 主进程入口 `index.ts` 接入 `startAudioProxy()`
7. 创建 `src/renderer/composables/useAudioEngine.ts`(HTMLAudioElement 封装:src 设置 / play/pause / timeupdate / ended)
8. 改造 `playback` store:瘦身为纯 UI 状态(`isPlaying` / `currentTime` / `volume` / `playMode`),`currentTrack` 由 `player` store 持有
9. `useAudioEngine` watch `playerStore.currentTrack` → 调 `window.api.playback.getAudioUrl` → `audio.src = url` → `play()`
10. `NowPlayingBar` / `PlayerView` 用 `useAudioEngine` 控制播放(替代现有假播放逻辑)
11. 处理音频流 URL 过期(播放失败时重新拉 URL)

**验收标准**:
- 点击收藏夹里的视频能播放出声音
- 进度条同步 `audio.currentTime`
- 播放/暂停/切歌正常
- 队列播放(单曲循环/列表循环/随机)正常
- 关闭应用重新打开,登录态保持(队列暂不恢复,阶段 6 做)

**至此"登录 → 拉收藏夹 → 播放音频"完整真实数据流打通,应用已可用。**

### 8.6 阶段 6:数据库接入 + 持久化(数据落库)

**目标**:把前面跑通的内存态数据落库,做持久化。此时已经验证过数据模型,可以放心建表。

**任务**:
1. 安装 `better-sqlite3` + `drizzle-orm` + `drizzle-kit` + `@types/better-sqlite3`
2. 跑 `pnpm exec electron-rebuild` 编译原生模块
3. 创建 `src/main/lib/db/schema.ts`(参考本计划 4.4,1:1 复刻 BBPlayer)
4. 创建 `src/main/lib/db/index.ts`(`initDb` + `getDb`,WAL 模式)
5. 配置 `drizzle.config.ts`,跑 `drizzle-kit generate` 生成迁移
6. 主进程入口接入 `initDb()`
7. 创建 `src/main/lib/services/playlistService.ts` / `trackService.ts` / `artistService.ts` / `bilibiliMetadataService.ts`(Drizzle CRUD)
8. 改造 `bilibiliPlaylistFacade`:收藏夹内容拉取后写 SQLite(事务 + `onConflictDoUpdate`)
9. 改造 `playbackFacade`:音频流 URL 缓存从内存 `Map` 迁到 `bilibiliMetadata` 表
10. 创建 `src/main/lib/services/queueService.ts` + `historyService.ts`,队列和播放历史落库
11. 创建 `src/main/ipc/playlist.ts` / `history.ts` handler(读 SQLite)
12. 渲染进程 `playlist` store / `queue` store 改为调 `window.api.playlist.*` / 读 SQLite
13. 删除 `src/renderer/data/` 假数据层(迁移完成)

**验收标准**:
- 应用启动后 SQLite 文件出现在 `app.getPath('userData')/bbplayer-desk.db`
- 用 Drizzle Studio 能看到收藏夹、曲目、播放历史表
- 收藏夹内容落库,重启后无需重新拉取
- 队列和播放历史跨重启恢复
- 现有 UI 行为不变(数据从 SQLite 读)

### 8.7 阶段 7+:剩余功能

**目标**:补全体验,按优先级:

- **阶段 7**:收藏夹增量同步 + UP 主投稿/合集同步(`bilibiliPlaylistFacade` 扩展,同步进度推送)
- **阶段 8**:歌词获取(多源匹配:网易云 > QQ > 酷狗 > B 站弹幕,`lyricService` + `LyricView` 接真实数据)
- **阶段 9**:搜索(视频/UP 主,WBI 签名,`SearchView`)
- **阶段 10**:外部歌单匹配(网易云歌单 → B 站,`externalPlaylist` facade)
- **阶段 11**:系统托盘 + 全局快捷键(可选)
- **阶段 12**:打包发布(electron-builder + 自动更新)

---

## 9. 风险与代价

### 9.1 主要风险

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| **Electron 学习曲线** | 主进程概念、IPC、BrowserWindow、preload、打包签名 | 优先读懂 electron-vite 脚手架代码;参考 BBPlayer 主进程逻辑(虽然是 RN 但概念互通) |
| **better-sqlite3 原生模块** | 需按 Electron 版本 rebuild,否则启动报错 | `@electron/rebuild` + `postinstall` 脚本;CI 打包时用 `electron-builder` 自动处理 |
| **主进程崩溃 = 应用崩溃** | 单点故障 | `app.on('render-process-gone')` 兜底;主进程逻辑保持简单,复杂业务放 facade |
| **IPC 性能** | 大数据量传输(如 1000 首歌)较慢 | 分页加载;或用 `SharedArrayBuffer`(高级场景) |
| **macOS 签名** | 未签名应用显示"未验证开发者"警告 | 学习项目可不签名;正式发布用 Apple Developer 账号 |
| **Electron 版本升级** | breaking change | 跟进官方 release notes;`electron-builder` 配合 |
| **数据库文件损坏** | 用户数据丢失 | WAL 模式 + 定期备份(可选) |

### 9.2 代价

| 项 | 量级 |
|---|---|
| 安装包体积 | 70-100MB(Chromium 70MB + Node + better-sqlite3 ~5MB) |
| 内存占用 | 200-300MB 起步 |
| 启动时间 | 1-3 秒(主进程启动 + Chromium 加载 + DB 初始化) |
| 首次配置 | electron-vite 脚手架 + better-sqlite3 rebuild 约 1-2 小时 |
| 跨平台构建 | macOS 需签名;Windows 需代码签名;Linux AppImage/deb |
| 分发 | 不能像 web 一样 URL 访问,需要安装包 |

### 9.3 开放问题

1. **是否需要多窗口?** —— 当前需求不需要,主窗口 + 模态即可
2. **是否需要系统托盘?** —— 可选,关闭窗口时最小化到托盘。阶段 8 之后考虑
3. **是否需要全局快捷键?** —— 可选,播放/暂停/上一首/下一首。阶段 9 之后考虑
4. **音频流代理 server 安全?** —— 只监听 `127.0.0.1`,不暴露外网;URL 参数加密(可选)
5. **数据库迁移策略?** —— Drizzle Kit 生成 SQL,应用启动时自动应用(检查 `PRAGMA user_version`)
6. **是否支持便携版?** —— 不安装,单 exe 解压即用。`electron-builder` 支持 portable target

---

## 10. 学习清单

启动前应当先读懂 BBPlayer 以下源码:

### 10.1 API client 层(必读,1:1 复刻)

| 文件 | 行数 | 重点学习 |
|---|---|---|
| `apps/mobile/src/lib/api/bilibili/client.ts` | ~60 | ApiClient 类、cookie 注入、ResultAsync 包装 |
| `apps/mobile/src/lib/api/bilibili/api.ts` | ~530 | 30+ 接口封装,重点是 `getAudioStream` 的 dolby/hi-res/normal 优先级 |
| `apps/mobile/src/lib/api/bilibili/wbi.ts` | ~80 | WBI 签名算法、mixinKeyEncTab、md5 |
| `apps/mobile/src/lib/api/bilibili/utils.ts` | ~50 | bv2av/av2bv 算法、CSRF 提取 |
| `apps/mobile/src/lib/api/netease/` | ~200 | 网易云 API client(用于歌词) |
| `apps/mobile/src/lib/api/qqmusic/` | ~200 | QQ 音乐 API client |
| `apps/mobile/src/lib/api/kugou/` | ~150 | 酷狗 API client |

### 10.2 service 层(必读,1:1 复刻)

| 文件 | 行数 | 重点学习 |
|---|---|---|
| `apps/mobile/src/lib/services/playlistService.ts` | ~150 | 单表 CRUD、事务注入、`withDB(tx)` 模式、Drizzle 关系查询 |
| `apps/mobile/src/lib/services/trackService.ts` | ~120 | 曲目池、跨歌单共享 |
| `apps/mobile/src/lib/services/artistService.ts` | ~80 | 艺术家反归一化 |
| `apps/mobile/src/lib/services/lyricService.ts` | ~200 | 多源匹配降级策略 |

### 10.3 facade 层(必读,1:1 复刻)

| 文件 | 行数 | 重点学习 |
|---|---|---|
| `apps/mobile/src/lib/facades/syncBilibiliPlaylist.ts` | ~400 | 收藏夹/合集/UP 主投稿同步,分页拉取 + 事务写入 |
| `apps/mobile/src/lib/facades/bilibili.ts` | ~200 | B 站 API 协调 |
| `apps/mobile/src/lib/facades/syncExternalPlaylist.ts` | ~300 | 网易云/QQ → B 站匹配 |

### 10.4 数据库层(必读,1:1 复刻)

| 文件 | 行数 | 重点学习 |
|---|---|---|
| `apps/mobile/src/lib/db/schema.ts` | ~150 | SQLite 表设计(Drizzle schema) |
| `apps/mobile/src/lib/db/index.ts` | ~30 | 数据库连接工厂 |

### 10.5 鉴权管理(必读)

| 文件 | 行数 | 重点学习 |
|---|---|---|
| `apps/mobile/src/hooks/stores/useAppStore.ts`(bilibiliCookie 部分) | ~200 | bilibiliCookie 状态机、三种登录方式、cookie 序列化 |
| `apps/mobile/src/features/auth/` | ~500 | 扫码登录 UI、Cookie 输入 Modal、登录流程编排 |

### 10.6 数据查询层(参考)

| 文件 | 行数 | 重点学习 |
|---|---|---|
| `apps/mobile/src/lib/config/queryClient.ts` | ~50 | QueryClient 配置、QueryCache 集中错误处理 |
| `apps/mobile/src/hooks/queries/bilibili/favorite.ts` | ~50 | useInfiniteQuery 模板 |
| `apps/mobile/src/hooks/queries/bilibili/video.ts` | ~40 | useQuery 模板 |

### 10.7 共享纯函数库(可直接复用)

| 文件 | 行数 | 重点学习 |
|---|---|---|
| `packages/splash/src/` | ~500 | 歌词解析/合并/时间换算,带测试,可直接复用 |
| `packages/splash/__tests__/` | ~300 | 测试模板 |

总计约 4000 行核心代码需要读懂。

---

## 11. 总结

### 11.1 核心原则

1. **无云后端**:所有数据获取和持久化都在 Electron 主进程,与 BBPlayer 一致
2. **主进程承载"后端"**:`api/clients` + `services` + `facades` + `db` 全在主进程
3. **渲染进程纯 UI**:Vue 应用 + Pinia(UI 状态)+ TanStack Query(缓存)+ `<audio>`(播放)
4. **IPC 作为唯一桥梁**:`contextBridge.exposeInMainWorld` 暴露安全 API
5. **1:1 复刻 BBPlayer**:由于都用 SQLite + Drizzle,service / facade / db 代码几乎逐行一致
6. **本地音频流代理**:主进程起 HTTP server 转发 B 站音频流,带 cookie/Referer

### 11.2 关键决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 架构 | Electron + electron-vite | 直接复刻 BBPlayer,无浏览器限制 |
| 主进程数据库 | better-sqlite3 + Drizzle ORM | 与 BBPlayer 完全一致,1:1 复刻 |
| HTTP client | undici(Node 原生 fetch) | Node 环境,无 CORS |
| Cookie 管理 | tough-cookie | Node 生态成熟,自动处理 domain/path/expires |
| 错误处理 | neverthrow | 与 BBPlayer 一致,ResultAsync 不 throw |
| IPC 模式 | `ipcRenderer.invoke` + 细粒度 handler | 类型清晰,组织方便 |
| 状态变更通知 | 主进程 `webContents.send` + 渲染 `ipcRenderer.on` | 主进程主动 push |
| 音频播放 | 本地 HTTP 代理 + `<audio>` | 主进程加 B 站请求头,渲染进程 `<audio>` 走本地 URL |
| 渲染进程状态 | Pinia(只存 UI 状态) | 严格与主进程数据分离 |
| 渲染进程缓存 | @tanstack/vue-query | 与 BBPlayer 一致 |

### 11.3 实施顺序(垂直打通)

1. **阶段 1** ✅:electron-vite 脚手架,迁移现有 Vue 代码
2. **阶段 2**:B 站 API client 基础(client + wbi + utils + P0 接口,不接 DB,appState 用 JSON 持久化)
3. **阶段 3**:登录流程打通(扫码 + 手动 cookie,真实账号登录)
4. **阶段 4**:收藏夹拉取与展示(数据先放内存,LibraryView/PlaylistView 接真实 B 站数据)
5. **阶段 5**:音频播放打通(streamProxy + useAudioEngine,点击即出声)
6. **阶段 6**:数据库接入 + 持久化(better-sqlite3 + Drizzle,内存数据落库,队列/历史跨重启恢复)
7. **阶段 7+**:增量同步 / 歌词 / 搜索 / 外部歌单匹配 / 托盘快捷键 / 打包发布

### 11.4 关键参考文件索引

| BBPlayer 文件 | 对应 BBPlayer-desk 主进程文件 | 作用 |
|---|---|---|
| `apps/mobile/src/lib/api/bilibili/client.ts` | `src/main/lib/api/clients/bilibili/client.ts` | B 站 API client |
| `apps/mobile/src/lib/api/bilibili/api.ts` | `src/main/lib/api/clients/bilibili/api.ts` | 30+ 接口封装 |
| `apps/mobile/src/lib/api/bilibili/wbi.ts` | `src/main/lib/api/clients/bilibili/wbi.ts` | WBI 签名 |
| `apps/mobile/src/lib/api/bilibili/utils.ts` | `src/main/lib/api/clients/bilibili/utils.ts` | bv2av / av2bv |
| `apps/mobile/src/lib/api/netease/` | `src/main/lib/api/clients/netease/` | 网易云 API |
| `apps/mobile/src/lib/api/qqmusic/` | `src/main/lib/api/clients/qqmusic/` | QQ 音乐 API |
| `apps/mobile/src/lib/api/kugou/` | `src/main/lib/api/clients/kugou/` | 酷狗 API |
| `apps/mobile/src/lib/services/playlistService.ts` | `src/main/lib/services/playlistService.ts` | 歌单 CRUD(1:1 复刻) |
| `apps/mobile/src/lib/services/trackService.ts` | `src/main/lib/services/trackService.ts` | 曲目 CRUD(1:1 复刻) |
| `apps/mobile/src/lib/services/lyricService.ts` | `src/main/lib/services/lyricService.ts` | 多源歌词匹配(1:1 复刻) |
| `apps/mobile/src/lib/facades/syncBilibiliPlaylist.ts` | `src/main/lib/facades/bilibiliPlaylist.ts` | B 站歌单同步(1:1 复刻) |
| `apps/mobile/src/lib/facades/syncExternalPlaylist.ts` | `src/main/lib/facades/externalPlaylist.ts` | 外部歌单匹配(1:1 复刻) |
| `apps/mobile/src/lib/config/queryClient.ts` | `src/renderer/lib/queryClient.ts`(渲染进程) | TanStack Query 配置 |
| `apps/mobile/src/hooks/queries/bilibili/*.ts` | `src/renderer/composables/queries/*.ts`(渲染进程) | 查询 hook 模板 |
| `apps/mobile/src/hooks/stores/useAppStore.ts`(bilibiliCookie 部分) | `src/main/lib/config/store.ts` + `src/main/lib/auth/` | B 站 cookie 管理 |
| `apps/mobile/src/lib/db/schema.ts` | `src/main/lib/db/schema.ts` | DB schema(1:1 复刻) |
| `apps/mobile/src/lib/db/index.ts` | `src/main/lib/db/index.ts` | DB 连接工厂(改 better-sqlite3) |
| `packages/splash/src/` | `src/renderer/utils/lrcParser.ts`(渲染进程)或 `src/main/lib/utils/lrcParser/` | 歌词解析纯函数 |
